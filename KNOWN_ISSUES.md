# Known Issues

Open items found while dry-running `RUN_ORDER.md` end to end and while auditing
this project for dead code / stale references. Fixed items are listed too, so this
doubles as a changelog of what's been verified.

Note: the project used to live one level down, under a `final_code/` folder inside this
repo - that folder was flattened away (everything moved up into the repo root) on
2026-08-24. Entries below written before that date may say `final_code/...`; read that
as the current repo root.

## Fixed and verified (re-ran the actual code, not just reasoned about it)

| Issue | File | Fix |
|---|---|---|
| `finetune_TrOCR.py` checked for a `final/` subfolder that never existed on disk (the checkpoint sits flat in `checkpoint/normal/trocr/`), so re-running it would silently start real training from scratch instead of refusing to overwrite | `train_model/normal/trocr/finetune_TrOCR.py` | Check now looks for `checkpoint/normal/trocr/model.safetensors` directly. Verified: running it now correctly raises `FileExistsError` and stops. |
| `results/*.csv` (`crnn_finetune_summary.csv`, `crnn_pilot_summary.csv`, `trocr_pilot_summary.csv`) stored absolute checkpoint paths from before the project got reorganized (`D:\...\finetune_preprocessing_compare\...`) | `data/doctor/results/*.csv` | Paths rewritten to the current `checkpoint/doctor/...` locations. Verified: all 8 paths resolve to real files. |
| `resolve_doctor_label_conflicts.py` used `pd.concat` to append its 6 hardcoded resolutions with no idempotency check - re-running it would duplicate those 6 rows in `labels.csv` | `previous_code/B_superseded_pipeline/doctor_dedup_v1/resolve_doctor_label_conflicts.py` (archived) | Dropped any existing row for the filename before re-adding it, at the time. Verified then: ran it twice back to back, `labels.csv` stayed at 10050 rows both times. Since fully superseded by `preprocessing/doctor/discover_doctor_duplicates.py` + `resolve_doctor_duplicates.py`, which don't need this patch - they're idempotent by construction (fresh dataframe each run, `drop_duplicates(keep="last")`). |
| `final_test_eval.py` unconditionally read `crnn_pilot_summary.csv` / `trocr_pilot_summary.csv` (the 15%-pilot results) and reported that as the "final" score - the CRNN number on record (test_cer=0.395) was actually the pilot (15%-trained) model's score, not the full-finetune model's | `train_model/doctor/final_test_eval.py` | Split into a shared `run_eval()` plus two calls: pilot-decision check (`final_test_scores_pilot.csv`) and full-finetune final score (`final_test_scores_full.csv`). Verified: real run produced correct pilot-stage output. |
| `pilot_compare.py` re-used `finetune_all.py`'s module-level `PIPELINES` (already narrowed to `["baseline"]`), so re-running the pilot silently stopped checking `clhaa` at all, contradicting its own docstring ("baseline vs clhaa") | `train_model/doctor/finetune_all.py`, `train_model/doctor/pilot_compare.py` | `run_crnn_all()` / `run_trocr_all()` now take an optional `pipelines=` param; `pilot_compare.py` passes its own `PILOT_PIPELINES` list. Verified: re-run now prints a real skip-check for `clhaa` (and now `fajardo`/`benitez` too, see below). |
| `data/Doctor/train.csv` / `val.csv` / `test.csv` (produced by the old, archived `split_Doctor.py`) were never read by any current script - dead weight left over from before the clean+split pipeline moved to `Doctor_dataset_prepare/` | `doctor/data/Doctor/` | Moved to `previous_code/B_superseded_pipeline/data_Doctor_old_split/`. |
| `final_test_eval.py`'s "2/2 full-finetune final score" step required *both* `crnn_finetune_summary.csv` and `trocr_finetune_summary.csv` to exist before evaluating *either* model, so CRNN's real, ready result was being skipped just because TrOCR's full finetune hasn't finished | `train_model/doctor/final_test_eval.py` | `run_eval()` now checks each model's results file independently. Verified: `data/doctor/results/final_test_scores_full.csv` currently contains only the CRNN row (TrOCR's summary file still doesn't exist), produced by a real run. |
| `app/streamlit_app.py` interpolated OCR output directly into HTML with `unsafe_allow_html=True` and no escaping (`raw_prediction`, `best_name` going straight into f-string `<span>` tags) | `app/streamlit_app.py` | Every interpolated value now goes through `html.escape()` first. |
| `app/streamlit_app.py`'s TrOCR inference call used the raw uploaded image instead of the preprocessed one, so turning the preprocessing toggle on wouldn't actually change what TrOCR sees once its own `clhaa` full finetune finishes and the toggle becomes live for it - the exact train/inference mismatch this whole redesign exists to prevent, just currently masked because TrOCR's toggle is disabled today and `baseline` preprocessing is a no-op anyway | `app/streamlit_app.py` | Changed `run_trocr(model, processor, device, image)` to `run_trocr(model, processor, device, image_for_model)`. No visible effect today (`baseline` = `img.copy()`), but stops the bug from resurfacing later. |
| `final_test_eval.py`'s `run_eval()` skipped recomputing `is_winner` for rows already written in a previous run - a row saved back when it was the only candidate (e.g. TrOCR `baseline` before `clhaa` finished training) kept `is_winner=True` forever, even after `clhaa` finished with a lower val CER and should have become the real winner. Both rows then read `is_winner=True` at once. | `train_model/doctor/final_test_eval.py` | Added `refresh_winners()`, called before each model's evaluation loop, recomputing `is_winner` for every already-recorded row from the current full candidate list. Verified: re-ran for real, TrOCR `baseline`'s `is_winner` flipped `True` -> `False` in `final_test_scores_full.csv`, `clhaa` correctly reads `True`. |
| No exception handling anywhere in the checkpoint-loading / results-CSV / user-upload code paths - a missing or corrupted checkpoint, a stale summary-CSV row pointing at a moved file, or a corrupted uploaded image would all surface as raw, unhandled tracebacks. | `train_model/doctor/final_test_eval.py`, `app/streamlit_app.py` | Added targeted `try/except` around checkpoint loading (`eval_crnn`/`eval_trocr`, `load_crnn`/`load_trocr`) and CSV reading (`pick_candidates`), each re-raising with a message naming the actual missing/stale path; wrapped the Streamlit image-upload `Image.open()`+`.load()` and the inference call so a bad upload shows an in-app error instead of crashing the app. Verified: `py_compile` clean, plus a real re-run of `final_test_eval.py` against the live CSVs. |
| `finetune_TrOCR.py` hardcoded `RESUME_FROM_CHECKPOINT = False`, requiring a manual code edit to resume after an interruption - and if someone instead hardcoded it to `True` for safety, a genuine first run (no checkpoint yet) would make the `Trainer` raise `"No valid checkpoint found"` instead of just training from scratch. | `train_model/normal/trocr/finetune_TrOCR.py` | Replaced the hardcoded flag with an auto-detect: `resume_from_checkpoint = True` only if a `checkpoint-*` folder already exists under `STAGE1_OUTPUT_DIR`, else `False`. A first run trains from scratch, a re-run after an interruption resumes - no manual edit needed either way. Verified: `py_compile` clean. |
| `finetune_all.py` baked the current machine's absolute path into `best_ckpt`/`final_model_dir` before writing it to the results CSVs (`os.path.abspath(__file__)`-derived), so `final_test_eval.py` (which reads that column and does `os.path.isfile()`/`os.path.isdir()` on it directly) would raise a misleading "summary CSV row is stale" error on any other machine, even though the checkpoint is really there, just at a different absolute path. | `train_model/doctor/finetune_all.py`, `train_model/doctor/final_test_eval.py` | Added `to_relative_path()`/`to_absolute_path()` anchored at `final_code/`; `finetune_all.py` now stores paths relative to that anchor, `final_test_eval.py` resolves them back to absolute (falling back to using the value as-is if it's already absolute, for old rows). Migrated the 4 existing results CSVs to the new relative format. Verified: `pick_candidates()` re-run for real, all 4 resolved paths point at real, existing files. |
| `run_eval()` evaluated every `FINAL_CANDIDATES` pipeline on `test.csv`, not just the validation-CER winner, even though this file's own docstring says "each of CRNN and TrOCR's **chosen winners** gets exactly one inference pass" (singular) - surfaced when fixing `final_test_scores_pilot.csv`'s stale schema (see below) caused `pick_candidates()` to find `clhaa` as a real, never-before-tested pilot candidate and start a genuine ~1.5h CPU inference run for it, for no purpose (the pilot checkpoint is never the production model, and nothing reads that number). | `train_model/doctor/final_test_eval.py` | Added a `winners_only` parameter that filters candidates down to `is_winner=True` before evaluating; on reflection, decided the pilot-decision check should not run at all (see the entry below), so `winners_only` is now only exercised by the archived script. Killed the in-progress inference before it produced any output; removed the one non-winner row (`CRNN`/`clhaa`) that had already been evaluated under the old behavior. |
| `final_test_eval.py`'s "pilot-decision check" step ran the pilot (15%-trained) winner through `test.csv` for no real purpose - that checkpoint is never the production model (the full-finetune one is), the decision it would "check" was already made and executed via validation CER alone, and the report never cites this number. It also had a stale schema bug: `final_test_scores_pilot.csv` was written before `run_eval()` was split into pilot/full calls and only had `model,winner_pipeline,val_cer,test_cer,test_exact_match_pct,n` (no `pipeline`/`is_winner` columns), so re-running `run_eval()` for real raised `KeyError: 'pipeline'`. | `train_model/doctor/final_test_eval.py` | Removed the pilot-decision-check call from `main()` entirely - `final_test_eval.py` now only runs the full-finetune final score. The call (with the `winners_only=True, include_wer=False` options that would have made it correct) is preserved, unrun, in `previous_code/C_decided_not_used/run_pilot_eval_only.py`; `final_test_scores_pilot.csv`'s last real content (the two original CRNN/TrOCR baseline rows) is archived alongside it. Verified: re-ran `final_test_eval.py` for real, it now only prints the full-finetune section and completes cleanly. |

## Deliberately left as-is (confirmed decisions, not bugs)

- **TrOCR doctor full finetune, `baseline`: manually finalized at epoch 9, not run to natural early-stopping.** Training was interrupted at epoch 11 for time (~2.4h/epoch on full data); `trainer_state.json`'s own `best_model_checkpoint` already pointed at epoch 9 (`checkpoint-3954`, eval CER 11.84%) - epochs 10-11 had already regressed (12.19%, 12.46%), the same signal `EarlyStoppingCallback` would eventually act on, just observed manually instead of waiting out the full patience window. `checkpoint/doctor/trocr/baseline/final/` was built from `checkpoint-3954` (the 10 model/processor files `trainer.save_model()`+`processor.save_pretrained()` would have produced, not the training-resume-only `optimizer.pt`/`scheduler.pt`/`rng_state.pth`/`trainer_state.json`), `DONE.txt` written, `trocr_finetune_summary.csv` created with this row. `clhaa` has since also finished (ran to its hard-capped epoch 11, `finetune_all.py`'s `max_epochs = 11 if pipeline_name == "clhaa" else 1000`) - both `trocr_finetune_summary.csv` rows and both `final_test_scores_full.csv` TrOCR rows are now real, complete results (`clhaa` reads as the winner by validation CER).
- **`checkpoint/doctor/crnn/clhaa/`** (originally the CRNN pilot's losing pipeline) is now a live, referenced checkpoint - `finetune_all.py`'s `PIPELINES` was expanded to `["baseline", "clhaa"]` and both were fully finetuned rather than betting everything on the pilot call alone, so `crnn_finetune_summary.csv` and `final_test_scores_full.csv` both carry real rows for it (CRNN's winner by validation CER is still `baseline`, val CER 0.2247 vs `clhaa`'s 0.2338). `fajardo`/`benitez`'s full-finetune checkpoints (and their two rows from this summary csv) remain archived to `previous_code/C_decided_not_used/doctor_crnn_full_finetune/` - CRNN's winner among *those* two was decided from the 15%-pilot comparison only, so they never got a 100%-data run.
- **`pilot_compare.py`'s `PILOT_PIPELINES = ["baseline", "clhaa", "fajardo", "benitez"]`** has since been run for real, for both CRNN and TrOCR - `crnn_pilot_summary.csv` and `trocr_pilot_summary.csv` each carry all 4 rows now.

## Still open - needs a decision

- **`IAM_WORDS_TXT`** in `preprocessing/normal/Iam_split.py` is a dead variable - defined, never read by any function in the project (confirmed: no code anywhere parses `words_new.txt` from scratch; the splits are all pre-computed CSVs). Candidate for deletion.
- **A fresh clone of this repo will not run end to end on its own** - resolved by handing over data separately, see below. `data/normal/I_Am_Dataset/`, `data/doctor/doctor_raw/images/`, `data/doctor/doctor_clean/images/`, `data/doctor/preprocessed_doctor/`, `data/doctor/preprocessed_doctor_test/` are all gitignored (too large for git). Scripts that only rely on cached results (`test_CrnnNormal.py`, `test_TrOCR.py`, `finetune_all.py`'s CRNN half, `pilot_compare.py`'s `baseline`/`clhaa`) happen to skip before ever touching images, so they're fine as-is. The rest need real files handed over directly (not re-derivable from anything in git):
  - `combineDataset.py` - `SOURCE_FOLDERS` points at 5 fixed paths right under `doctor_raw/` itself (`doctor_raw/89/`, `doctor_raw/RxHand Original/`, `doctor_raw/Doctor's Handwritten Prescription BD dataset/{Testing,Training,Validation}/`), from 3 actual raw zips (the BD dataset's one zip unpacks into 3 of those 5 paths). **Place the 3 zips directly in `doctor_raw/`, named after each dataset (e.g. `RxHand Original.zip`), and run the script - `ensure_source_folders_extracted()` auto-unzips them** (handles each zip's different internal packaging; the "Doctor Handwriting Recognition Dataset" zip extracts into a folder literally named `89`, not its own name - see that function's docstring for why). Verified against the live `doctor_raw/images/`+`labels.csv`: byte-for-byte identical, including every image file. These 5 extracted folders (and the raw zips themselves) are gitignored, same as `doctor_raw/images/`. If you don't have those 3 raw zips handy, easier to skip this step entirely: its output, `doctor_raw/images/`, is what actually needs to be handed over (58MB) - place it at `data/doctor/doctor_raw/images/`, `labels.csv` is already in git, then skip straight to `discover_doctor_duplicates.py`.
  - `discover_doctor_duplicates.py`, `generate_doctor_preprocessed.py` - need `doctor_raw/images/` and `doctor_clean/images/` respectively, handed over directly (see table below).
  - `finetune_TrOCR.py` - its "already trained, refuse to overwrite" check (see the ① fix above) now points at the right file, but that file (`checkpoint/normal/trocr/model.safetensors`, 1.27GB) is itself on the >100MB gitignore list, so a fresh clone still won't have it and the check will still (correctly, not a bug this time) conclude no model exists yet and start training from scratch unless it's handed over too. Its resume flag is now auto-detected (see the fix table below), so an interrupted run just needs the script re-launched as-is, no manual flag edit needed.
- **`checkpoint/doctor/trocr/baseline/final/model.safetensors` and similar (>100MB) files** are gitignored (GitHub's hard push limit) and need a separate transfer method (Git LFS, cloud storage, manual copy) - not `git push`. Full list and sizes (the old per-epoch numbered `checkpoint-N/` folders, including their `optimizer.pt`, are no longer part of this list - they were training-intermediate duplicates of these same `final/` folders and have been archived to `previous_code/`, see its `README.md`):
  - `checkpoint/normal/trocr/model.safetensors` (1273.9 MB)
  - `checkpoint/doctor/trocr/baseline/final/model.safetensors` (1273.9 MB)
  - `checkpoint/doctor/trocr/clhaa/final/model.safetensors` (1273.9 MB)
  - `checkpoint/doctor/trocr_pilot/baseline/final/model.safetensors` (1273.9 MB)
  - `checkpoint/doctor/trocr_pilot/clhaa/final/model.safetensors` (1273.9 MB)
  - `checkpoint/doctor/trocr_pilot/fajardo/final/model.safetensors` (1273.9 MB)
  - `checkpoint/doctor/trocr_pilot/benitez/final/model.safetensors` (1273.9 MB)

## Images that need to be handed over separately (gitignored, not in git)

| Folder | Size | Files |
|---|---|---|
| `data/normal/I_Am_Dataset/` (IAM zip + extracted images) | 1,490.8 MB | 34,248 |
| `data/doctor/doctor_raw/images/` | 58.2 MB | 10,347 |
| `data/doctor/doctor_clean/images/` | 56.9 MB | 10,050 |
| `data/doctor/preprocessed_doctor/` | 90.2 MB | 32,160 |
| `data/doctor/preprocessed_doctor_test/` (baseline + clhaa - the two models' respective winners) | 25 MB | 4,020 |

**Total ≈ 1.7 GB**, plus the checkpoint files listed above (~8.9 GB, 7 files) if the recipient needs those too.
